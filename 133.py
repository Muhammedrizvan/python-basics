#intersections

#keep only the duplicates
#the intersections() method will return a new set
#that only contains the items that are present in the both set

#join set1 an dset2 ,but keep only the duplicate

set1 = {"apple","banana","cheery"}
set2 = {"orange","kiwi","apple"}

set3 = set1.intersection(set2)
print(set3)
