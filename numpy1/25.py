#get the data type of an array containing tring?

import numpy as np
arr = np.array(['apple','banana','cherry'])
print(arr.dtype)

#U :this indicates that the data type is a unicode string
#6:this specified the length of the unicode strng in the array
